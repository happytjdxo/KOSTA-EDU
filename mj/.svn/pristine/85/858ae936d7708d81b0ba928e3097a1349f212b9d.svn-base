package condition.book.test;
/*
 *  Variable의 종류 ::
 *  Member Variable(Field) / Local Variable
 *  
 *  선언된 위치에 따라서 구분된다.
 *  Field -- 1) 클래스 바로 아래 / 메소드 블럭 밖에 선언된다.
 *  		 2) 기본값이 있다... 초기화 하지 않고 바로 사용해도 에러 안난다.
 *  		 3) Heap 영역에 저장된다.
 *  		 4) Class의 식구, 멤버라 하고 클래스가 메모리에 살아있는 동안 유지된다.
 *  Local Variable
 *  	 --  1) 메소드 블럭 안에서 선언된다.
 *  		 2) 기본값이 없다.. 초기화 하지 않고 사용하면 컴파일시 에러 난다.
 *  		 3) Stack 영역에 저장된다.
 *  		 4) 메소드가 { 열리면 생성되었다가 } 닫히면 자동적으로 메모리에서 내려온다.
 *  		 5) Temporary, Automatically V라고도 한다.
 *  
 */
public class FlowExamTest1 {
	public static void main(String[] args) {
		int visitCount = 0;	//Local Variable
		if(visitCount<1)
			System.out.println("처음 방문 하셨군요!!");
		else
			System.out.println("방문한 적이 있으시군요!!");


	}//main
}
