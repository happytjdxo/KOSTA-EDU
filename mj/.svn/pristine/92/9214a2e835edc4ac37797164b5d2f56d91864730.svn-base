package condition.step2.test;


/*
 * 해당 성적별로 학점을 출력하는 소스 코드를 작성
 * 이때 제어문에 사용하는 if, else if, else 라는 키워드를 모두 사용.
 */
public class ScoreIfTest {
	public static void main(String[] args) {
		int score = 78;
		/*
		 * 성적이 90~100 점인 경우는 A학점을 출력
		 * 성적이 80~89 점인 경우는 B학점을 출력
		 * 성적이 70~79 점인 경우는 C학점을 출력
		 * 성적이 그외인 경우는 "Try Again"을 출력
		 */
		
		
		//성적조회 프로그램
		int sc = 80;
		
		//1.70점 이상이면 합격.
		if(sc>=70){
			System.out.println("합격");
		}
		//2.70점 이상이면 합격, 70점 미만이면 불합격.
		if(sc>=70){
			System.out.println("합격");
		}else{
			System.out.println("불합격");
		}
		//3.70점이상이면 합격, 70점 미만 60이상이면 미달, 60점미만이면 불합격.
		if(sc>=70){
			
		}else if(sc<70 && sc>=60){
			
		}else{
			
		}
		
				
		if (score>=90 && score<=100){
			System.out.println("당신의 학점은 A 입니다.");
		}else if(score>=80 && score<90) {
			System.out.println("당신의 학점은 B 입니다.");
		}else if(score>=70 && score<80) {
			System.out.println("당신의 학점은 C 입니다.");
		}else {
			System.out.println("Try Again!!");
		}
	}

}
/*
 *  해당 코드의 문제점 :
 *  78이라는 성적이 소스코드에 하드코딩 되어져 있다.
 *  이렇게 되면 이 프로그램은 78점일 때에만 돌아가는 프로그램이 된다.
 *  
 *  해결책 : 
 *  값이 컴파일 시점에 입력되어 돌아가도록 하지 말고
 *  실행 시점에 그때마다 다른 값을 넣을 수 있고 그 값이 실행될 수 있도록
 *  변수로 처리 할 수 있어야 한다.
 *  
 *  
 *  어떤 사람에 대한 성적인지도 알 수 있도록 처리해 주자.
 */
//값을 컴파일할때 돌아가는게 문제임.(값이 하드코딩 되어져 있음 -> 컴파일할때 값이 돌아감. )
















//if (score >= 90){
//	System.out.println("당신의 학점은 A 입니다.");
//}else if(score <=89) {
//	System.out.println("당신의 학점은 B 입니다.");
//}else if(score <=79) {
//	System.out.println("당신의 학점은 C 입니다.");
//}else if(score <=69) {
//	System.out.println("Try Again!!");
//}