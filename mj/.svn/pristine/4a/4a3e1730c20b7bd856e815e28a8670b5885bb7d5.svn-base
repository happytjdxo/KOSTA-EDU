import java.io.*;
import java.util.*;

public class IOProb1 { // 퍼블릭으로 클래스 생성.
	public static void main(String[] args) throws Exception{ // 발생하는 에러는 JVM에서 처리하도록 한다.
		//구현하세요...
		String fileName = "src/data/score.txt";
		
		// 파일 입력 스트림을 만들어야되. 기본, 보조. 선
		
		// 보조를 통해서 한 라인씩 읽어와야되. 저장할 공간이 필요해. 이때, 체크해.(타입)
		
		// 출력을 해야되.
		
		// subString을 통해서 필요한 부분만 짤라 내야되.
		
		// 타입 체크해서 그릇에 담아. 알아서 선언해.
		
		// 최종적으로 출력해.
		
		/*
		 * Byte Char 차이> 
		 * Byte - Stream. > Object[Input/Output]Stream / File[Input/Output]Stream / Buffered [Input/Output] Stream
		 * Char - Reader. > File[Reader or Writer] / Buffered [Reader/Writer]
		 * 
		 * 기본스트림 - 파일에 관계된 깔대기. 인자를 파일만 받아.(String s, File f)
		 * 보조스트림 - 기본스트림과 관계된 깔대기. 인자를 기본스트림.(Stream, Reader, Writer)
		 * API > Stream 생성자 > 인자값으로 분류 무엇이 기본스트림이고 무엇이 보조스트림인지.
		 * 
		 * Reader 기본스트림 > Reader 보조스트림 // Writer 기본스트림 > Writer 보조스트림
		 * InputStream 기본스트림 > InputStream 보조스트림 // OutputStream 기본스트림 > OutputStream 보조스트림.
		 */
		BufferedReader br = new BufferedReader( );
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		/*String fileName = "src/data/score.txt"; // 파일의 경로 및 파일 명을 입력한 변수. 오류 가능성을 줄이기 위해서 이러한 방식으로 사용.
		//FileInputStream fi = new FileInputStream(fileName);			//stream으로 끝나는 stream은 bite형
		FileReader fr = new FileReader(fileName);						// reader or Writer 로 끝나는 Stream은 char 형
		BufferedReader br = new BufferedReader(fr); // br에 fr의 인자를 넣음 fr은 fileName -> "src/data/score.txt"; 이다.
		// BufferedReader br = new BufferedReader(new FileReader(fileName));
		String line;	//얜 왜씀?? readLine()으로 읽어들인 값을 임시적으로 저장하기 위한 공간.
		int total = 0; // 총점 구하기 위한 변수
		int cnt = 0; // 라인의 수(여기서는 사람 수)를 구하기 위한 변수
		while((line = br.readLine())!=null) // readLine()메서드를 사용해서 읽은 파일의 각 라인을 읽어오는데, 매번 바뀌므로 line이라는 변수에 저장하고, null일 경우에는 while을 벗어난다.
		{
			System.out.println(line); // 한 줄 읽어서 저장했던 line 변수를 출력한다.
			total += Integer.parseInt(line.substring(line.indexOf(":")+1, line.length()));
			// 각 총점을 구한다, line에 있는 subString이라는 메서드를 사용하는것.
			// .length() 의 리턴형은?	--> 리턴형은 int
			// .indexOf() 의 리턴형과 사용법은?	--> 리턴형은 int형이고 (처음 만나는 : 의 문자열을 찾는다.)
			// subString() 의 인자가 무엇이고, 사용법은 무엇인가. 리턴형이 무엇인가.(리턴형은 String이고, 인자는 line.indexOf(":")+1, line.length()) 고 , 사용법은 :
			// 해당 라인(18번)에 대한 개략적인 설명을 스스로 하시오.
			cnt++;
		}
		System.out.println("총점 : " +total);
		System.out.println("평균 : " +total/cnt);
*/
	}
}
/*
		BufferedReader br = new BufferedReader(new FileReader("src/data/score.txt"));

		String[] splitLine = null;
		String line = null;
		int total = 0;
		int data = 0;

		while((line = br.readLine())!= null) {
			splitLine = line.split(":");
			total += Integer.parseInt(splitLine[1]);
			System.out.println(line);
			total += data;
		}
		System.out.println("총점 : " +total);
		System.out.println("평균 : " +(double)total/5);
 */